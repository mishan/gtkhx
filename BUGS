High priority:

- Cannot actually select fonts in font chooser. The inputs (keyboard and mouse) are ignored. I cannot even close
  the font chooser dialog until I first close the Settings dialog. So something is going on with Settings holding onto input.

Gtk-Message: 21:05:47.173: GtkDialog mapped without a transient parent. This is discouraged.

Medium priority:

(none)

Low priority:

- The connection to hlserver.com eventually gets closed for some reason (possibly server timeout). If it is
  a server activity timeout, it would be nice to indicate that

Won't Fix:

- Window position saving does not work on Wayland
