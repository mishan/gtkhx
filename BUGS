High priority:

- Loading the tracker crashes
- Connecting with icons.rsrc present in /usr/local/share/gtkhx crashes
- Chat window does not initially display. It works after closing and reopening it

Low priority:

- The connection to hlserver.com eventually gets closed for some reason (possibly server timeout). If it is
  a server activity timeout, it would be nice to indicate that