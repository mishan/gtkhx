High priority:

- The names in the Private Message xtext widget are not split out into the left column, so the separator does not work
- Icons wider than "standard" width but narrower than the banner format don't have sufficient spacing between icon and name
  (note: The fix for this is probably just adding more spacing, regardless of icon type)

Medium priority:

(none)

Low priority:

- The connection to hlserver.com eventually gets closed for some reason (possibly server timeout). If it is
  a server activity timeout, it would be nice to indicate that

Won't Fix:

- Window position saving does not work on Wayland. GTK+ 4.24 might have an API for saving sessions
