High priority:

(none — all earlier high-priority items resolved this session: bookmarks
not working / split-button first-click / right-click popup actions /
non-ASCII server name corruption / file preview lockup)

Medium priority:

(none — chat-window minimum-width and right-click CSS-node-insert-after
warning resolved this session)

Low priority:

- The connection to hlserver.com eventually gets closed for some reason (possibly server timeout). If it is
  a server activity timeout, it would be nice to indicate that

Won't Fix:

- Window position saving does not work on Wayland
