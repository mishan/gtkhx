High priority:

- Issue with fetching banners with HOPE ciphers enabled

Medium priority:

- Toasts display for a while and old toasts may still display after connecting to a new server. Suggestion: clear old toasts after a new connection is initiated

Low priority:

- The connection to hlserver.com eventually gets closed for some reason (possibly server timeout). If it is
  a server activity timeout, it would be nice to indicate that

Won't Fix:

- Window position saving does not work on Wayland. GTK+ 4.24 might have an API for saving sessions
