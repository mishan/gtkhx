High priority:

- File Previews do not work, and they lock up the application
- Icons are showing up twice in Settings (two identical rows per icon)

Medium priority:

- Right clicking on a user in the Users list gives a warning:

(gtkhx:285643): Gtk-CRITICAL **: 19:06:59.176: gtk_css_node_insert_after: assertion 'previous_sibling == NULL || previous_sibling->parent == parent' failed

Low priority:

- The connection to hlserver.com eventually gets closed for some reason (possibly server timeout). If it is
  a server activity timeout, it would be nice to indicate that

Won't Fix:

- Window position saving does not work on Wayland
