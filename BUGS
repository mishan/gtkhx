High priority:

- Flatpak build broken. Missing `cargo` and probably the rest of the Rust toolchain
- Crash when inviting oneself to a second Private Chat

Medium priority:

(none)

Low priority:

- The connection to hlserver.com eventually gets closed for some reason (possibly server timeout). If it is
  a server activity timeout, it would be nice to indicate that.
- hlserver.com broadcasts to us "0 command(s) at a time" on login.
