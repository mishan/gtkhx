[package]
name = "hxrand"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"
description = "Cryptographic RNG for GtkHx — replaces src/rand.c"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
getrandom = "0.2"

[build-dependencies]
cbindgen = "0.27"
